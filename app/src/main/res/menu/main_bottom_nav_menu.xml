<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/destination_profile"
        android:icon="@drawable/profile_selector"
        app:showAsAction="ifRoom"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/destination_calendar"
        android:icon="@drawable/calendar_selector"
        app:showAsAction="ifRoom"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/destination_home"
        android:icon="@drawable/home_selector"
        app:showAsAction="ifRoom"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/destination_articles"
        android:icon="@drawable/articles_selector"
        app:showAsAction="ifRoom"
        tools:ignore="MenuTitle" />
    <item
        android:id="@+id/destination_analyzes"
        android:icon="@drawable/analyzes_selector"
        app:showAsAction="ifRoom"
        tools:ignore="MenuTitle" />

</menu>